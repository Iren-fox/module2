my_list = [42, 69, 0, 322, 13, 0, 99, -5, 9, 8, 7, -6, 5]
y = 0
while y <= len(my_list): #пока текущий индекс элемента меньше количества элементов
	    if my_list[y] > 0: #если элемент больше 0
	    	print (my_list[y]) #печать этого элемента
	    	y = y+1 #переход к следующему элементу
	    elif my_list[y] == 0: #если элемент равен 0
	    	y = y+1 #переход к следующему элементу
	    else: #если элемент не больше и не равен нулю, т.е. меньше нуля, 
	    	break #конец цикла
